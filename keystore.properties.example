# Copy this file to `keystore.properties` (do NOT commit real secrets).
# Replace the values below with your keystore details.

# Absolute or relative path to the keystore file (relative to project root is recommended)
storeFile=keystore/my-release-key.jks
storePassword=changeit
keyAlias=offlineNavKey
keyPassword=changeit
